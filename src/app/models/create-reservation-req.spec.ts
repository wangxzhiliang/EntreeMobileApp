import { CreateReservationReq } from './create-reservation-req';

describe('CreateReservationReq', () => {
  it('should create an instance', () => {
    expect(new CreateReservationReq()).toBeTruthy();
  });
});
