import { GetAvailableTablesReq } from './get-available-tables-req';

describe('GetAvailableTablesReq', () => {
  it('should create an instance', () => {
    expect(new GetAvailableTablesReq()).toBeTruthy();
  });
});
