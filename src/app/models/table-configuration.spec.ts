import { TableConfiguration } from './table-configuration';

describe('TableConfiguration', () => {
  it('should create an instance', () => {
    expect(new TableConfiguration()).toBeTruthy();
  });
});
